# M1 determinism evidence

Two independent `AssemblyM1Pipeline` compilations of `template-block-pair.firmament` produce the same definition specialization identities, transforms, residuals, topology metrics, and `AssemblyGeometryArtifactIr.DeterministicSha256`.

Canonical recorded artifact hash: `28CDA0D2A40F2CA2C41E3A99CCAB6543B43764412B679A25D9050B2B659F949E`.

The generated inspection artifact is `executable-assembly.json`; performance timings are deliberately excluded from the deterministic payload.

