# Humanoid research fixtures

`adult-standard-v1.fixture.json` is the compact tracked identity of the **NONCANONICAL X0 synthetic regression topology**. Its historical ID is retained for replay, not production admission. Generate the full artifact into ignored local output with the recorded command; ordinary unit tests construct it in memory and never fetch third-party data.

`antonia-reference.sidecar.json` records the exact external reference and the narrow research-fit scope. It does not contain Antonia geometry and does not upgrade the unresolved conversion-component scope identified by HUMANOID-RECON-X0. A fit result remains research evidence until that gate is reviewed.

`x0-registration-config.json` freezes the first deterministic fitting baseline. Raw qualification output belongs under `artifacts/local/humanoid-x0/`.

`antonia-original.sidecar.json` admits the original author's Antonia 1.2.0 OBJ,
independently of the unadmitted CharMorph conversion. The adjacent original
license notice is preserved byte-for-byte. X1's source-pose and prepared
surface candidates retain original polygon IDs and a fixed binding diagonal;
neither is a promoted canonical runtime artifact. See
`docs/release/HUMANOID-X1.md` and the current research-tool guide at
`docs/public/humanoid-research.md`. Reproduce with
`pwsh -File scripts/qualify-humanoid-x1.ps1`.
