# CTC-01 sections

The X2 normalization probe uses `aetheris sections` with `Z ± 0.001 mm`.
The X3 Firmament composition continues to provide normalized slab-region IR;
the STEP route is being brought to the same loop contract.

Current evidence: the raised central hexagon at Z=5 and Z=50 normalizes to one
closed exact loop. Lower levels emit typed topology diagnostics rather than
provisional chains because face partitions and cylindrical seams create
high-valence/coincident boundary supports. Those diagnostics are the required
input to the next normalization increment; no loop correspondence or boundary
deviation is asserted from them.

## X2 stabilization status

`aetheris sections` now reports raw fragment provenance, canonical/atomic
counts, collapsed duplicates, phase timings, and typed diagnostics per
Below/At/Above sample. The lower CTC sections remain intentionally rejected:
the trace identifies coincident fragments with conflicting material-left
orientation (for example faces 90 and 91 at Z=-100), while four non-axis-normal
conical faces are typed unsupported because their horizontal intersections are
general conics. Neither case is converted into a provisional loop or region.

Deferred before correspondence: resolve face/coedge ownership for coincident
fragments, finish nesting/material-region construction for conflict-free STEP
graphs, and decide whether general conics remain permanently rejected or gain
their own exact section route. Sections diagnose construction; whole-body
deviation audits final fidelity.
