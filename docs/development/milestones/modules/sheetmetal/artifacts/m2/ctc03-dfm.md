# CTC-03 bounded DFM

Parameterized checks cover positive thickness, minimum inside-radius/thickness, cut-boundary-to-bend distance, cut-boundary-to-source-edge distance, and flat-region overlap. Subjects use reconstructed names and findings can carry a minimum displacement suggestion; suggestions are never applied automatically.

CTC-03 passes the default experimental policy: R/t is 3.333 on each bend, the two vent slots clear recovered bend/edge limits, and the flat IR reports no planar-region overlap. No universal shop/material claim is made. Corner collision and relief sizing remain not evaluated where recovery only provides `Unknown` corner evidence.
