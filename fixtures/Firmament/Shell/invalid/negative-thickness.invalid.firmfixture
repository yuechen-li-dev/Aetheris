// fixture-id: shell-negative-thickness
// case: shell-negative-thickness
// category: Shell
// validity: invalid
// implementation: rejected
// expected: invalid
// expected-stage: rejected
// expected-diagnostic: firmament-shell-thickness-non-positive
// notes: AIR-FIRMAMENT-A1 metadata-only corpus fixture; syntax may be future design intent where parser support is not yet settled.

solid box = Box(width: 10, depth: 8, height: 6);
shell box remove box.face("+Z") thickness -1;
