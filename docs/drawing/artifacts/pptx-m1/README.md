# AETHERIS-PPTX-COLLAB-M0 drawing evidence

This directory is generated from the canonical Bearing Block Part and Machine/BearingModule Assembly fixtures through the real `aetheris drawing compile` path. It contains DrawingIR, SVG, immutable vector PDF, native editable A4 PPTX, validation JSON, ReviewIR, A4 Review PPTX, and a 16:9 DFM deck.

The package contains native lines, text boxes, groups, and tables, with no raster image parts. Shape identity is semantic and deterministic. LibreOffice 26.2 headless imported every PPTX and exported all slides to PDF for visual QA.
