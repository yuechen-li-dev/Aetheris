# CTC-01 LLM pressure X5 — evidence gate

The new compiler-owned `Slot<Capsule>` route is exercised by the semantic capsule fixture. It replaces four authored boundary segments, four tangent/closure obligations, and hand-maintained derived end points with `Center`, `Direction`, `Length`, `Width`, and `Extent`.

CTC X5 is deliberately not authored yet. Existing repository evidence is internally inconsistent with the requested target: `ctc01-reconstruction-a1.md` identifies the **left** R20 capsule using end centers `(-325, 0)` and `(-245, 0)` (width 40 mm, overall length 120 mm), but calls the **right** opening an R10 rounded-rectangle candidate. No reference STEP/section artifact in this checkout resolves the right opening as an R20 capsule or establishes its Z extent. Writing `ctc01_prismatic_blockout_x5.firmament` as a right-hand capsule would therefore be manual drafting under a semantic name, not a reconstruction.

The proven simple-fixture removal is `2856.6370614359173 mm² × 20 mm = 57132.74122871835 mm³`; exact line/arc lowering, selection correspondence, and inspect output are covered. The next convergent CTC action is to extract the right-opening section loop and its host Z span; if it resolves as capsule, author X5 with the generic. If it resolves as rounded rectangle, implement that separate family rather than misclassifying it.
