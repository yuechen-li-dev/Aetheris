# Test support

This directory contains auxiliary projects and assets consumed by the real `Aetheris.*.Tests` suites. It is not the repository's test-suite root.

`Aetheris.TestForgePack` and its duplicate provide controlled external assemblies; `PublicPackageSmoke` is copied into a clean directory to validate packed public libraries.
