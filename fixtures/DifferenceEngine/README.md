# Difference Engine reusable source

`parts/Storage.firmament` contains ordinary parameterized part definitions used by the typed storage author and canonical regression fixtures. It is an include catalog, not a standalone build root.

See the [public guide](../../docs/public/firmament/difference-engine.md). Generated Subassembly roots, STEP and meshes belong under ignored `artifacts/local/`.
