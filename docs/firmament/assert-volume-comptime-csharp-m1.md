# Assert Volume M1 scope update

The earlier compile-time-C# proposal is deferred: this checkout has no bounded
embedded-C# host, and the trusted Forge DLL loader is intentionally unsuitable
for source execution. M1 therefore uses literal, typed volume values only.

See [Assert Volume M1](assert-volume-m1.md) for the implemented contract.
