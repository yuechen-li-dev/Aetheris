# Negative diagnostic evidence

`Aetheris.Semantics.Tests` covers scalar-to-Profile, body-to-boundary, false
Forge Profile claim, missing/private member, raw face member, mesh ID region,
stable-ID collision, and exact Forge member consumers. Existing parser tests
cover duplicate recognized members and unresolved imported faces. Specific codes
are asserted rather than message fragments where a common code exists.
