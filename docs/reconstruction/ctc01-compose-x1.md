# CTC-01 compose X1 status

CTC-01 is no longer blocked by the core proper-overlap arrangement primitive. `Compose` now resolves active Profiles into one analytic line/arc arrangement before producing one section-stack topology plan; it does not create temporary operation solids. A bounded CTC reconstruction remains deferred because the source-analysis evidence has not yet isolated an Observed/StrongInference prismatic subset worth promoting.

The executable raised-pad proof has critical levels `0, 10, 15`, slab areas `400, 64`, analytic volume `4320 mm³`, and independent in-memory BRep volume `4319.999999999998 mm³`. Its exported STEP (`artifacts/raised-pad-compose.step`) reimports as one enclosed manifold body.

The shallow-pocket proof now has analytic, in-memory, and STEP-reimported mass `3808 mm³` (reimport result `3807.9999999999995`). The contained through-cut has the corresponding four-way material evidence at `3640 mm³` (reimport result `3639.9999999999995`). The repaired artifacts are topology-enclosed and orientation-consistent under independent BRep mass evaluation. Consequently no CTC profiles, observed Z-levels, or reference-equivalence claims are promoted by this increment: the remaining CTC gate is exact proper-overlap line/arc arrangement.

The mixed line/arc fixture now proves transverse line/circle splitting through analytic STEP round-trip. The remaining CTC pressure-test blocker is not the arrangement kernel but a high-confidence observed/strong-inference CTC subset whose source profile can be stated without inventing named silhouette geometry; no complete CTC claim is made.
