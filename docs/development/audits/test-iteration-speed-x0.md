# Kernel test iteration speed, 2026-09-22

Run `scripts/Test-KernelIteration.ps1` for Core, Firmament, and Modules tests during geometry/language iteration. It excludes `Category=SlowCorpus` whole-model corpus qualification; `scripts/Test-KernelIteration.ps1 -IncludeSlowCorpus` includes those cases. A full solution qualification remains `dotnet test Aetheris.slnx --no-build --no-restore -m:1` after a solution build. The script fails on the first failing project and does not write tracked output.

The measured preclassification Core run (`Category!=SlowCorpus` with only the older category labels) passed 1,032 tests in 4m57s. Its TRX profile, retained in ignored `artifacts/local/test-profiles/core-iteration-profile.trx`, showed 20–58-second NIST import/export cases repeated across regression classes. After tagging the measured whole-file cases, the same Core filter passed 958 tests in 51 seconds. Small STEP unit tests remain in that filter. The full Firmament project passed 1,576 tests in 2m08s when run alone; after moving measured whole-model coil, gear, knot, and wire-form cases to `SlowCorpus`, its iteration filter passed 1,563 tests in 47 seconds. The test profile is `artifacts/local/test-profiles/firmament-profile.trx`.

Two wall-clock ratio tests in `RecognizedConstructionRecipeTests` rebuilt identical through-holes 380 times and could fail on scheduling alone; nearby tests already compare direct, legacy, and facade topology and STEP artifacts. The V8 annular-profile failure came from assigning the synthetic segment name `Boundary` to both traced circles; full-circle guides now inherit their authored names, while real cross-loop name collisions still fail. An assembly display exporter preflight repeated pcurve work with a builder that did not support mixed cylinder/B-spline bodies; the actual tessellator and its face-count checks remain authoritative. Display fallback tests now pass an explicit 30-second *test* budget instead of relying on the 5-second production default under parallel test load.

The actual iteration script passed 958 Core, 1,563 Firmament, and 103 Modules tests in 119 seconds including project checks. Focused validation also passed: 7 display fallback tests, 3 FTC07 view tests, 38 profile-pipeline tests, the V8 physical engine test, the plateau mesh test, and the updated module B-spline contract test. `dotnet build Aetheris.slnx --no-restore -m:1` passed with seven pre-existing WebAssembly warnings; a subsequent serial full-solution test run passed 3,836 tests in 20 test projects with zero failures (Core 1,099/1,099 in 8m39s and Firmament 1,578/1,578 in 1m37s). Its ignored log is `artifacts/local/test-profiles/full-serial.log`. The faster filter is an iteration gate, not a substitute for full corpus qualification.
